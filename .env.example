# ClickHouse Configuration
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DATABASE=elixir_index
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Local Sourcify (optional - for self-hosted Sourcify)
SOURCIFY_API_URL=http://localhost:5555

# Sourcify Proxy URLs for IP rotation (comma-separated)
# Deploy workers using: cd infra/cloudflare-worker && ./deploy.sh --count 3
# Leave empty to use direct Sourcify API (may hit rate limits)
SOURCIFY_PROXY_URLS=

# Supported Chains (comma separated IDs)
SUPPORTED_CHAINS=1,56

# RPC URLs (Format: RPC_URL_{CHAIN_ID})
RPC_URL_1=https://eth.llamarpc.com
RPC_URL_56=https://binance.llamarpc.com
