# Elixir Indexer Configuration
# Copy this file to .env and customize

# ============================================================================
# Chain Configuration
# ============================================================================
# Format: CHAINS="chain_id=rpc_url,chain_id=rpc_url"
CHAINS="1=https://rpc.ankr.com/eth,137=https://rpc.ankr.com/polygon"

# ============================================================================
# ClickHouse Configuration
# ============================================================================
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DATABASE=default
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# ============================================================================
# Sourcify Configuration
# ============================================================================

# Proxy URLs for IP rotation (comma-separated)
# Deploy workers using: cd infra/cloudflare-worker && ./deploy.sh --count 3
# Then list your worker URLs here:
SOURCIFY_PROXY_URLS=https://sourcify-proxy-1.yoursubdomain.workers.dev,https://sourcify-proxy-2.yoursubdomain.workers.dev

# Direct Sourcify URL (used as fallback or when no proxies configured)
SOURCIFY_DIRECT_URL=https://sourcify.dev/server

# Request timeout in milliseconds
SOURCIFY_TIMEOUT=30000

# Maximum retry attempts on failure
SOURCIFY_MAX_RETRIES=3

# ABI cache time-to-live in milliseconds (default: 24 hours)
SOURCIFY_CACHE_TTL=86400000
