{
  "name": "sourcify-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Sourcify API with IP rotation",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
